<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
        <fullName>Address_country_must_match_account_count</fullName>
        <active>true</active>
        <description>Country on address should match country of related account</description>
        <errorConditionFormula>TEXT(Country__c)  &lt;&gt;  TEXT(Account__r.Country__c)</errorConditionFormula>
        <errorDisplayField>Country__c</errorDisplayField>
        <errorMessage>Country on address should match country of related account</errorMessage>
    </ValidationRule>
